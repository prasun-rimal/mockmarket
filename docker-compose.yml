services:
  postgres:
    image: postgres:16-alpine
    container_name: mockmarket-postgres
    environment:
      POSTGRES_DB: mockmarket
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d mockmarket"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
